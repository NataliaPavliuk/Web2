/*function join(array, separator = '') {//map, reduce, join
    var result = "";
    for (let i = 0; i < array.length - 1; i++) {
          result += array[i];
          result += separator;
    }
    result += array[array.length - 1];
    return result;
  };*/

  let arr = ["Natalia", "Victoria", "Maria"];
  console .log(arr.join("-"));

 